# 01 - C++ 基础语法

## 📚 本章内容

学习 C++ 的基本语法，包括变量、数据类型、控制流程等。

### 示例文件

1. **01_hello_world.cpp** - 第一个 C++ 程序
2. **02_variables_types.cpp** - 变量和数据类型
3. **03_input_output.cpp** - 输入输出操作
4. **04_control_flow.cpp** - 控制流程（if/switch/循环）
5. **05_arrays.cpp** - 数组和 vector

---

## 🚀 如何编译运行

### 方法一：单个文件编译
```bash
# 编译
g++ -std=c++17 01_hello_world.cpp -o hello_world

# 运行
./hello_world
```

### 方法二：编译所有文件
```bash
# 编译所有 cpp 文件
g++ -std=c++17 *.cpp -o all_basics

# 运行
./all_basics
```

### 方法三：使用 Makefile（推荐）
```bash
# 如果有 Makefile
make
./program_name
```

---

## 💡 学习重点

### 与 Java/Kotlin 的主要区别

| 特性 | Java/Kotlin | C++ |
|------|-------------|-----|
| 包含库 | `import java.util.*` | `#include <iostream>` |
| 输出 | `System.out.println()` | `cout << ... << endl` |
| 输入 | `Scanner.nextLine()` | `cin >>` 或 `getline()` |
| 数组 | `int[] arr = new int[5]` | `int arr[5]` |
| 动态数组 | `ArrayList<Integer>` | `vector<int>` |
| 字符串 | `String` | `string` |
| 自动推导 | Kotlin: `val x = 42` | `auto x = 42` |

### 重要概念

1. **头文件**：类似 Java 的 import，但使用 `#include`
2. **命名空间**：`using namespace std` 避免写 `std::cout`
3. **编译型语言**：需要先编译再运行（不像 Java 有 JVM）
4. **类型推导**：`auto` 关键字，类似 Kotlin 的类型推导
5. **范围for循环**：`for (int x : arr)` 类似 Java 增强for循环

---

## 🎯 练习建议

1. **运行每个示例**，观察输出结果
2. **修改代码**，实验不同的值和逻辑
3. **对比理解**：结合 Java/Kotlin 经验理解差异
4. **动手写**：不要只看，一定要自己敲一遍

---

## 📖 扩展学习

- 完成本章后，继续学习 **02_functions_pointers**
- 重点关注指针概念，这是 C++ 与 Java 最大的区别
- 建议在线练习：[LeetCode](https://leetcode.cn/) 的 C++ 简单题

---

**下一章：函数和指针 ➡️**
