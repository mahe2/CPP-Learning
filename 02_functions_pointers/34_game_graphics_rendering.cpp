/**
 * 游戏画面渲染机制详解
 * 
 * 核心问题：
 * 1. 如果 C++ 用于游戏开发，游戏画面也是 C++ 做的吗？
 * 2. 王者荣耀、原神这样的游戏，画面是怎么实现的？
 * 3. C++ 在游戏渲染中扮演什么角色？
 */

#include <iostream>
#include <string>
#include <vector>

using namespace std;

// ============================================
// 1. 游戏画面渲染的完整技术栈
// ============================================

void explain_game_rendering_stack() {
    cout << "\n" << string(70, '=') << endl;
    cout << "1. 游戏画面渲染的完整技术栈" << endl;
    cout << string(70, '=') << endl;
    
    cout << R"(
【答案：是的，但不是纯 C++】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

游戏画面的渲染涉及多层技术栈：

┌─────────────────────────────────────────────────────┐
│ 层级 5：游戏逻辑层                                   │
│         C++ 或 C# (Unity) 或 蓝图 (Unreal)          │
│         - 角色控制、技能系统、AI                     │
│         - 调用渲染 API 绘制画面                      │
├─────────────────────────────────────────────────────┤
│ 层级 4：游戏引擎层                                   │
│         C++ (Unreal/自研) 或 C# (Unity)             │
│         - 场景管理、资源加载                         │
│         - 物理引擎、动画系统                         │
│         - 封装底层图形 API                           │
├─────────────────────────────────────────────────────┤
│ 层级 3：图形 API 层                                  │
│         C/C++ 封装                                   │
│         - OpenGL / OpenGL ES (移动端)               │
│         - Vulkan (现代高性能)                        │
│         - DirectX (Windows)                         │
│         - Metal (iOS/macOS)                         │
├─────────────────────────────────────────────────────┤
│ 层级 2：GPU 驱动层                                   │
│         C/C++ (显卡厂商提供)                         │
│         - NVIDIA、AMD、Mali、Adreno 驱动             │
│         - 将 API 调用翻译成 GPU 指令                 │
├─────────────────────────────────────────────────────┤
│ 层级 1：GPU 硬件层                                   │
│         硬件电路 (不涉及编程语言)                    │
│         - 顶点着色器、片段着色器                     │
│         - 光栅化、纹理映射                           │
│         - 执行 Shader 代码                           │
└─────────────────────────────────────────────────────┘


【关键点】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. C++ 负责什么？
   ✅ 游戏逻辑（角色、技能、AI）
   ✅ 引擎核心（场景管理、资源加载）
   ✅ 调用图形 API（告诉 GPU 画什么）
   
2. GPU 负责什么？
   ✅ 实际的画面渲染（像素计算）
   ✅ 执行 Shader 代码（光照、特效）
   ✅ 并行处理（每秒百万次计算）

3. Shader 是什么？
   ✅ 在 GPU 上运行的小程序
   ✅ 使用专门语言：GLSL (OpenGL)、HLSL (DirectX)、MSL (Metal)
   ✅ 控制每个像素的颜色、光照、特效

4. 流程总结：
   C++ 代码 → 调用图形 API → GPU 驱动 → GPU 执行 Shader → 画面输出


【类比理解】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

把游戏渲染比作拍电影：

C++ 游戏代码    = 导演（决定拍什么、怎么拍）
图形 API        = 摄影指令（镜头位置、角度、灯光）
GPU 驱动        = 摄影师（执行导演指令）
GPU 硬件        = 摄影机（实际拍摄）
Shader 代码     = 后期特效（调色、滤镜、光晕）
最终画面        = 电影画面

所以：
- C++ 不直接"画"每个像素
- C++ 告诉 GPU "画什么、在哪画、用什么材质"
- GPU 负责实际的像素渲染工作

)" << endl;
}

// ============================================
// 2. 王者荣耀的技术架构
// ============================================

void explain_honor_of_kings_architecture() {
    cout << "\n" << string(70, '=') << endl;
    cout << "2. 王者荣耀的技术架构" << endl;
    cout << string(70, '=') << endl;
    
    cout << R"(
【王者荣耀技术栈】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

客户端（你手机上运行的）：
┌────────────────────────────────────────────┐
│ 游戏引擎：Unity (C# + C++)                 │
│                                            │
│ 游戏逻辑：C# (Unity脚本)                   │
│   ├─ 角色控制（移动、技能释放）            │
│   ├─ UI 系统（按钮、血条、小地图）         │
│   ├─ 特效触发（技能特效、伤害数字）        │
│   └─ 网络通信（与服务器同步）              │
│                                            │
│ 引擎核心：C++ (Unity 底层)                 │
│   ├─ 场景管理（地图、角色、道具）          │
│   ├─ 物理引擎（碰撞检测）                  │
│   ├─ 动画系统（角色动作、表情）            │
│   └─ 渲染管线（调用图形 API）              │
│                                            │
│ 图形 API：                                 │
│   ├─ iOS: Metal (苹果官方)                │
│   └─ Android: OpenGL ES / Vulkan          │
│                                            │
│ Shader：GLSL/HLSL/MSL                      │
│   ├─ 角色皮肤材质（金属、布料、皮肤）      │
│   ├─ 光照效果（环境光、点光源）            │
│   ├─ 特效渲染（火焰、闪电、烟雾）          │
│   └─ 后处理（辉光、景深、色调映射）        │
│                                            │
│ GPU：手机芯片内的图形处理器                │
│   ├─ 高通 Adreno (骁龙)                    │
│   ├─ ARM Mali (联发科、海思)               │
│   └─ Apple GPU (A/M 系列芯片)              │
└────────────────────────────────────────────┘

服务器端（腾讯机房）：
┌────────────────────────────────────────────┐
│ 游戏服务器：C++ (高性能)                   │
│   ├─ 战斗逻辑同步                          │
│   ├─ 伤害计算                              │
│   ├─ 技能冷却                              │
│   └─ 位置校验（防外挂）                    │
│                                            │
│ 登录/匹配服务：C++ 或 Go                   │
│ 聊天服务：C++ (即时通讯)                   │
│ 数据库：MySQL + Redis                      │
└────────────────────────────────────────────┘


【画面渲染的完整流程】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

以"露娜释放大招"为例：

1️⃣ 游戏逻辑层 (C#)
   ┌────────────────────────────────────────┐
   │ // 玩家点击大招按钮                     │
   │ void OnUltimateButtonClick() {         │
   │     // C# 代码                         │
   │     hero.CastUltimate();              │
   │     // 触发动画                        │
   │     animator.Play("Ultimate");        │
   │     // 触发特效                        │
   │     PlayEffect("LunaUltimate");       │
   │     // 通知服务器                      │
   │     SendToServer(SKILL_ULTIMATE);     │
   │ }                                     │
   └────────────────────────────────────────┘

2️⃣ 引擎层 (Unity C++)
   ┌────────────────────────────────────────┐
   │ - 加载角色模型（3D mesh）               │
   │ - 加载特效粒子系统                      │
   │ - 更新动画骨骼                          │
   │ - 计算光照位置                          │
   │ - 准备渲染数据                          │
   └────────────────────────────────────────┘

3️⃣ 图形 API 调用 (C++)
   ┌────────────────────────────────────────┐
   │ // C++ 伪代码                          │
   │ glBindBuffer(VERTEX_BUFFER, lunaModel);│
   │ glUseProgram(lunaShader);             │
   │ glUniformMatrix4fv(transform, ...);   │
   │ glDrawElements(TRIANGLES, ...);       │
   │                                       │
   │ // 翻译：                              │
   │ // "GPU，请用露娜的模型、材质、位置，  │
   │ //  绘制她的大招动作"                  │
   └────────────────────────────────────────┘

4️⃣ GPU 驱动层 (C/C++)
   ┌────────────────────────────────────────┐
   │ - 将 API 调用翻译成 GPU 指令            │
   │ - 上传顶点数据到显存                    │
   │ - 上传纹理贴图                          │
   │ - 配置渲染管线状态                      │
   └────────────────────────────────────────┘

5️⃣ GPU 执行 Shader (GLSL)
   ┌────────────────────────────────────────┐
   │ // 顶点着色器（计算每个顶点位置）       │
   │ void main() {                          │
   │     gl_Position = projection * view *  │
   │                   model * position;    │
   │ }                                      │
   │                                        │
   │ // 片段着色器（计算每个像素颜色）       │
   │ void main() {                          │
   │     vec3 color = texture(skinTexture, uv);│
   │     color += lightColor * intensity;   │
   │     gl_FragColor = vec4(color, 1.0);   │
   │ }                                      │
   └────────────────────────────────────────┘

6️⃣ 最终画面输出
   ┌────────────────────────────────────────┐
   │ - GPU 并行处理所有像素                  │
   │ - 1080p 屏幕 = 2,073,600 个像素         │
   │ - 每秒 60 帧 = 124,416,000 像素/秒      │
   │ - 输出到屏幕显示                        │
   └────────────────────────────────────────┘


【关键技术点】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C# 做什么（游戏逻辑）：
  ✅ 监听玩家输入（点击、滑动）
  ✅ 控制角色行为（移动、攻击）
  ✅ 管理游戏状态（血量、金币）
  ✅ 触发动画和特效
  ✅ 网络通信

C++ 做什么（引擎核心）：
  ✅ 场景图管理（哪些对象在画面中）
  ✅ 资源加载（模型、纹理、音效）
  ✅ 物理计算（碰撞、重力）
  ✅ 动画插值（骨骼动画）
  ✅ 调用图形 API（告诉 GPU 画什么）

Shader 做什么（GPU 程序）：
  ✅ 顶点变换（3D 坐标 → 屏幕坐标）
  ✅ 光照计算（漫反射、镜面反射）
  ✅ 纹理采样（给模型贴皮肤）
  ✅ 特效实现（水面、火焰、辉光）
  ✅ 后处理（模糊、景深、色彩校正）

GPU 做什么（硬件加速）：
  ✅ 并行处理数百万个像素
  ✅ 每秒渲染 60+ 帧画面
  ✅ 实时计算光照和阴影
  ✅ 处理复杂的数学运算

)" << endl;
}

// ============================================
// 3. 原神的技术架构（更复杂的例子）
// ============================================

void explain_genshin_impact_architecture() {
    cout << "\n" << string(70, '=') << endl;
    cout << "3. 原神的技术架构（更复杂）" << endl;
    cout << string(70, '=') << endl;
    
    cout << R"(
【原神技术栈】（比王者荣耀更复杂）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

客户端（PC/手机/PS5）：
┌────────────────────────────────────────────┐
│ 游戏引擎：Unity (米哈游深度定制)           │
│                                            │
│ 游戏逻辑：C#                               │
│   ├─ 开放世界探索                          │
│   ├─ 元素反应系统                          │
│   ├─ 任务系统                              │
│   └─ 多人联机                              │
│                                            │
│ 引擎核心：C++ (定制优化)                   │
│   ├─ 大世界流式加载（边走边加载）          │
│   ├─ LOD 系统（远处简化模型）              │
│   ├─ 遮挡剔除（不画看不见的东西）          │
│   ├─ 地形系统（程序生成地形）              │
│   └─ 天气系统（实时光照变化）              │
│                                            │
│ 高级渲染技术（C++ + Shader）：             │
│   ├─ 物理基础渲染 (PBR)                    │
│   ├─ 全局光照 (GI)                         │
│   ├─ 体积光（阳光穿过树叶）                │
│   ├─ 屏幕空间反射 (SSR)                    │
│   ├─ 动态天气（雨、雪、雾）                │
│   └─ 卡通渲染（二次元风格）                │
│                                            │
│ 图形 API：                                 │
│   ├─ PC: DirectX 11/12、Vulkan            │
│   ├─ iOS: Metal                           │
│   ├─ Android: OpenGL ES、Vulkan           │
│   └─ PS5: GNM (索尼定制)                   │
└────────────────────────────────────────────┘


【原神画面渲染的特色】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 开放世界渲染（技术难点）
┌────────────────────────────────────────────┐
│ 问题：地图太大，无法一次加载全部           │
│                                            │
│ 解决方案（C++ 实现）：                     │
│ ✅ 流式加载：                              │
│    - 只加载玩家周围区域                    │
│    - 离开区域自动卸载                      │
│    - 预加载前方区域（避免卡顿）            │
│                                            │
│ ✅ LOD (Level of Detail)：                 │
│    - 远处物体用低模（少三角形）            │
│    - 近处物体用高模（细节丰富）            │
│    - 自动切换，玩家无感知                  │
│                                            │
│ ✅ 遮挡剔除：                              │
│    - 不渲染被山、建筑遮挡的对象            │
│    - 节省 GPU 性能                         │
│                                            │
│ ✅ 天空盒优化：                            │
│    - 天空用立方体贴图（不是真实模型）      │
│    - 远山用贴图（不是 3D 模型）            │
└────────────────────────────────────────────┘

2. 卡通渲染（二次元风格）
┌────────────────────────────────────────────┐
│ 实现方式（Shader 代码）：                  │
│                                            │
│ ✅ 描边效果：                              │
│    - 检测边缘，画黑色轮廓线                │
│    - 使人物有"手绘"感                      │
│                                            │
│ ✅ 阴影简化：                              │
│    - 不用真实渐变阴影                      │
│    - 用明暗两色分割（cel-shading）         │
│    - 更像动漫风格                          │
│                                            │
│ ✅ 高光特化：                              │
│    - 头发有特殊高光形状                    │
│    - 眼睛有"星星"反射                      │
│                                            │
│ // 简化的卡通 Shader 伪代码                │
│ float light = dot(normal, lightDir);       │
│ if (light > 0.5)                           │
│     color = brightColor;  // 亮面          │
│ else                                       │
│     color = darkColor;    // 暗面          │
└────────────────────────────────────────────┘

3. 元素特效（水火雷冰风岩草）
┌────────────────────────────────────────────┐
│ 粒子系统（C++ 管理，GPU 渲染）：           │
│                                            │
│ ✅ 火元素：                                │
│    - 数千个粒子模拟火焰                    │
│    - Shader 实现燃烧、扭曲效果             │
│                                            │
│ ✅ 水元素：                                │
│    - 流体模拟（简化版）                    │
│    - 反射、折射 Shader                     │
│                                            │
│ ✅ 雷元素：                                │
│    - 电弧生成算法（C++）                   │
│    - 辉光、闪烁 Shader                     │
│                                            │
│ ✅ 元素反应：                              │
│    - C# 计算哪些元素组合                   │
│    - C++ 触发对应特效                      │
│    - GPU 渲染爆炸、融化等效果              │
└────────────────────────────────────────────┘


【性能优化技术】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

为什么手机能运行原神？（技术黑魔法）

1. 动态分辨率（C++ 实现）
   - 性能不足时降低渲染分辨率
   - 保持帧率流畅
   - 例如：从 1080p 降到 720p

2. 动态光影质量
   - 高端机：实时阴影 + 全局光照
   - 中端机：简化阴影
   - 低端机：烘焙阴影（预计算）

3. 帧率自适应
   - 检测 GPU 负载
   - 自动调整特效数量
   - 减少草、树的数量

4. 多线程渲染（C++）
   - CPU 多核并行处理
   - 一核处理游戏逻辑
   - 其他核处理渲染准备

5. GPU 实例化（Instancing）
   - 一次调用渲染多个相同物体
   - 例如：草地上的草（几千株）

)" << endl;
}

// ============================================
// 4. 不同游戏类型的技术对比
// ============================================

void compare_game_types() {
    cout << "\n" << string(70, '=') << endl;
    cout << "4. 不同游戏类型的技术对比" << endl;
    cout << string(70, '=') << endl;
    
    cout << R"(
【王者荣耀 vs 原神 vs 其他游戏】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────┐
│ 游戏类型 │ 引擎    │ 逻辑语言 │ 渲染复杂度 │ 性能要求  │
├──────────┼─────────┼──────────┼────────────┼─────────┤
│ 王者荣耀 │ Unity   │ C#       │ ⚡⚡       │ 中低     │
│          │         │          │ MOBA 俯视  │ 手机流畅 │
│          │         │          │ 简化特效   │          │
├──────────┼─────────┼──────────┼────────────┼─────────┤
│ 原神     │ Unity   │ C#       │ ⚡⚡⚡⚡⚡  │ 高       │
│          │ (定制)  │          │ 开放世界   │ 要求强   │
│          │         │          │ PBR渲染    │ 显卡     │
├──────────┼─────────┼──────────┼────────────┼─────────┤
│ 和平精英 │ Unreal  │ C++      │ ⚡⚡⚡⚡    │ 中高     │
│ (PUBG)   │ 4       │ (蓝图)   │ 大地图     │          │
│          │         │          │ 真实渲染   │          │
├──────────┼─────────┼──────────┼────────────┼─────────┤
│ 黑神话   │ Unreal  │ C++      │ ⚡⚡⚡⚡⚡  │ 极高     │
│ 悟空     │ 5       │ (蓝图)   │ 电影级画质 │ RTX 4090│
│          │         │          │ 光追       │ 才流畅   │
├──────────┼─────────┼──────────┼────────────┼─────────┤
│ 我的世界 │ C++     │ C++      │ ⚡         │ 低       │
│ (基岩版) │ (自研)  │          │ 方块风格   │ 手机可玩 │
│          │         │          │ 简单光照   │          │
└─────────────────────────────────────────────────────┘


【王者荣耀：性能优先】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

为什么画面相对简单？
  ✅ 目标：低端手机也能 60 帧流畅
  ✅ 优化策略：
     - 固定俯视角度（不需要远景）
     - 地图小（5v5 竞技场）
     - 角色模型简化（面数少）
     - 特效控制（不能太炫影响操作）
     - 快速匹配（几秒进游戏）

渲染技术：
  ⚡ 前向渲染（简单快速）
  ⚡ 简化光照（环境光 + 主光源）
  ⚡ 低精度阴影
  ⚡ 粒子特效优化（数量少）

目标玩家：
  📱 千元机也能玩
  📱 全民竞技


【原神：画面与性能平衡】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

为什么画面精美但手机也能跑？
  ✅ 目标：主机级画面 + 手机可玩
  ✅ 优化策略：
     - 动态分辨率缩放
     - 激进的 LOD 系统
     - 延迟渲染（PC/主机）
     - 前向渲染（手机）
     - 多档画质设置

渲染技术：
  ⚡⚡⚡⚡⚡ PBR 材质系统
  ⚡⚡⚡⚡⚡ 全局光照（烘焙 + 实时）
  ⚡⚡⚡⚡⚡ 体积光、体积雾
  ⚡⚡⚡⚡⚡ 屏幕空间反射
  ⚡⚡⚡⚡ 动态天气

目标玩家：
  🎮 PC/PS5：主机级体验
  📱 旗舰手机：高画质
  📱 中端手机：中低画质但流畅


【黑神话悟空：画面极致】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

为什么画面顶级但要求高？
  ✅ 目标：电影级画质，不考虑手机
  ✅ 技术栈：
     - Unreal Engine 5
     - Nanite（亿级多边形）
     - Lumen（实时全局光照）
     - 光线追踪（RTX）
     - MetaHuman（真实角色）

渲染技术：
  ⚡⚡⚡⚡⚡ 全局光线追踪
  ⚡⚡⚡⚡⚡ Nanite 虚拟几何
  ⚡⚡⚡⚡⚡ Lumen 动态 GI
  ⚡⚡⚡⚡⚡ 高精度毛发渲染
  ⚡⚡⚡⚡⚡ 电影级后处理

目标玩家：
  💻 RTX 4070 以上 PC
  🎮 PS5 主机
  ❌ 手机：不可能


【技术演进趋势】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2018-2020：
  王者荣耀、和平精英
  → 追求流畅，画面够用

2020-2023：
  原神、崩坏星穹铁道
  → 手机高画质成为可能

2023-2025：
  黑神话悟空、幻兽帕鲁
  → PC 主机级画质

未来（2025+）：
  → 手机也能跑接近 PC 画质（芯片性能提升）
  → 云游戏（服务器渲染，手机只接收视频流）
  → AI 辅助渲染（DLSS、FSR 超分辨率）

)" << endl;
}

// ============================================
// 5. C++ 在游戏渲染中的实际代码示例
// ============================================

void show_cpp_rendering_code() {
    cout << "\n" << string(70, '=') << endl;
    cout << "5. C++ 在游戏渲染中的实际代码" << endl;
    cout << string(70, '=') << endl;
    
    cout << R"(
【简化的游戏渲染循环】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// C++ 游戏主循环（60 FPS）
void GameLoop() {
    while (isRunning) {
        // 1. 处理输入（16ms 内完成所有工作）
        ProcessInput();
        
        // 2. 更新游戏逻辑
        UpdateGameLogic(deltaTime);
        
        // 3. 准备渲染数据
        PrepareRenderData();
        
        // 4. 调用渲染
        RenderFrame();
        
        // 5. 显示到屏幕
        SwapBuffers();
        
        // 6. 计算帧时间
        deltaTime = CalculateDeltaTime();
    }
}


【示例：渲染一个角色】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// C++ 代码（游戏引擎层）
class Character {
private:
    Mesh* mesh;           // 3D 模型（顶点、面）
    Texture* texture;     // 皮肤贴图
    Shader* shader;       // 渲染程序
    Transform transform;  // 位置、旋转、缩放
    
public:
    void Render(Camera* camera) {
        // 1. 激活 Shader 程序
        shader->Use();
        
        // 2. 设置变换矩阵
        shader->SetMatrix("model", transform.GetMatrix());
        shader->SetMatrix("view", camera->GetViewMatrix());
        shader->SetMatrix("projection", camera->GetProjectionMatrix());
        
        // 3. 绑定纹理
        texture->Bind(0);
        shader->SetInt("textureSampler", 0);
        
        // 4. 绘制模型
        mesh->Draw();
    }
};


【底层图形 API 调用】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// OpenGL 示例（C/C++）
void Mesh::Draw() {
    // 绑定顶点数组对象
    glBindVertexArray(VAO);
    
    // 绘制三角形
    glDrawElements(GL_TRIANGLES, indexCount, GL_UNSIGNED_INT, 0);
    
    // 解绑
    glBindVertexArray(0);
}


【Shader 代码（GPU 上运行）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// 顶点着色器（GLSL）
#version 330 core
layout(location = 0) in vec3 position;  // 顶点位置
layout(location = 1) in vec2 texCoord;  // 纹理坐标
layout(location = 2) in vec3 normal;    // 法线

uniform mat4 model;       // 模型矩阵
uniform mat4 view;        // 视图矩阵
uniform mat4 projection;  // 投影矩阵

out vec2 TexCoord;
out vec3 Normal;
out vec3 FragPos;

void main() {
    // 计算最终顶点位置
    gl_Position = projection * view * model * vec4(position, 1.0);
    
    // 传递数据到片段着色器
    TexCoord = texCoord;
    Normal = mat3(transpose(inverse(model))) * normal;
    FragPos = vec3(model * vec4(position, 1.0));
}


// 片段着色器（GLSL）
#version 330 core
in vec2 TexCoord;
in vec3 Normal;
in vec3 FragPos;

uniform sampler2D textureSampler;  // 皮肤纹理
uniform vec3 lightPos;             // 光源位置
uniform vec3 viewPos;              // 摄像机位置

out vec4 FragColor;

void main() {
    // 1. 采样纹理颜色
    vec3 color = texture(textureSampler, TexCoord).rgb;
    
    // 2. 计算环境光
    vec3 ambient = 0.3 * color;
    
    // 3. 计算漫反射
    vec3 norm = normalize(Normal);
    vec3 lightDir = normalize(lightPos - FragPos);
    float diff = max(dot(norm, lightDir), 0.0);
    vec3 diffuse = diff * color;
    
    // 4. 计算镜面反射
    vec3 viewDir = normalize(viewPos - FragPos);
    vec3 reflectDir = reflect(-lightDir, norm);
    float spec = pow(max(dot(viewDir, reflectDir), 0.0), 32);
    vec3 specular = spec * vec3(1.0);
    
    // 5. 最终颜色
    vec3 result = ambient + diffuse + specular;
    FragColor = vec4(result, 1.0);
}


【特效系统示例】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// C++ 粒子系统
class ParticleSystem {
private:
    struct Particle {
        glm::vec3 position;
        glm::vec3 velocity;
        glm::vec4 color;
        float lifetime;
    };
    
    std::vector<Particle> particles;
    
public:
    void Update(float deltaTime) {
        for (auto& p : particles) {
            // 更新位置
            p.position += p.velocity * deltaTime;
            
            // 更新生命周期
            p.lifetime -= deltaTime;
            
            // 渐隐效果
            p.color.a = p.lifetime / maxLifetime;
            
            // 移除死亡粒子
            if (p.lifetime <= 0) {
                // 标记删除
            }
        }
    }
    
    void Render() {
        // 使用粒子 Shader
        particleShader->Use();
        
        // 渲染每个粒子（通常用实例化渲染优化）
        for (const auto& p : particles) {
            particleShader->SetVec3("position", p.position);
            particleShader->SetVec4("color", p.color);
            particleMesh->Draw();
        }
    }
};


【性能分析】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

60 FPS = 16.67ms 每帧

时间分配（典型游戏）：
  2ms  - 处理输入
  4ms  - 游戏逻辑（AI、物理、网络）
  3ms  - 渲染准备（C++ CPU 工作）
  7ms  - GPU 渲染（Shader 执行）
  0.67ms - 缓冲区交换
  ─────
  16.67ms 总计

如果超过 16.67ms：
  → 掉帧（卡顿）
  → 需要优化

)" << endl;
}

// ============================================
// 6. 总结与学习路径
// ============================================

void summary_and_learning_path() {
    cout << "\n" << string(70, '=') << endl;
    cout << "6. 总结与学习路径" << endl;
    cout << string(70, '=') << endl;
    
    cout << R"(
【核心总结】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❓ 游戏画面是 C++ 做的吗？

✅ 是，但不完全是：

1. C++ 负责（引擎层）：
   ✅ 调用图形 API（告诉 GPU 画什么）
   ✅ 管理场景（哪些对象要渲染）
   ✅ 资源加载（模型、纹理）
   ✅ 动画系统（骨骼动画）
   ✅ 物理引擎（碰撞、重力）
   ✅ 优化算法（LOD、遮挡剔除）

2. Shader 负责（GPU）：
   ✅ 实际的像素渲染
   ✅ 光照计算
   ✅ 特效实现
   ✅ 后处理效果

3. C# 负责（Unity 游戏）：
   ✅ 游戏逻辑
   ✅ UI 系统
   ✅ 触发渲染

所以：
  C++ 是"导演"（决定画什么）
  Shader 是"画师"（实际画出来）
  GPU 是"画板"（执行绘画）


【王者荣耀画面实现】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

技术栈：
  ┌──────────────────────────────┐
  │ 玩家看到的画面                │
  ├──────────────────────────────┤
  │ ↑ GPU 渲染（Shader）          │
  ├──────────────────────────────┤
  │ ↑ OpenGL ES API (C++)        │
  ├──────────────────────────────┤
  │ ↑ Unity 引擎核心 (C++)       │
  ├──────────────────────────────┤
  │ ↑ 游戏逻辑 (C#)               │
  └──────────────────────────────┘

流程：
  C# 代码说："露娜释放大招"
  ↓
  Unity C++ 引擎说："加载露娜模型和大招特效"
  ↓
  OpenGL API 说："GPU，请渲染这些数据"
  ↓
  GPU 执行 Shader 说："我开始画每个像素"
  ↓
  屏幕显示：露娜的炫酷大招画面！


【不同角色的分工】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

游戏程序员（C++/C#）：
  ✅ 写游戏逻辑
  ✅ 调用引擎 API
  ✅ 优化性能
  ✅ 不需要懂底层图形学

引擎程序员（C++）：
  ✅ 开发游戏引擎
  ✅ 封装图形 API
  ✅ 实现渲染管线
  ✅ 需要深入理解图形学

图形程序员（C++ + Shader）：
  ✅ 写 Shader 代码
  ✅ 实现特效算法
  ✅ 优化渲染性能
  ✅ 需要精通数学和图形学

技术美术（TA）：
  ✅ 调整 Shader 参数
  ✅ 优化材质
  ✅ 设计视觉效果
  ✅ 需要懂美术和技术


【学习路径建议】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果你想做游戏开发：

入门路径（推荐 Unity）：
  1️⃣ 学 C# 基础（1-2 周）
  2️⃣ 学 Unity 引擎（1-2 月）
     - 场景编辑
     - 脚本编写
     - UI 系统
  3️⃣ 做小游戏练习（2-3 月）
     - 2D 平台跳跃游戏
     - 简单 3D 游戏
  4️⃣ 学习进阶技术
     - 动画系统
     - 粒子特效
     - 基础 Shader

进阶路径（如果想深入图形）：
  1️⃣ 学习 C++ 高级特性（3-6 月）
  2️⃣ 学习图形学基础（3-6 月）
     - 线性代数
     - 3D 数学
     - 渲染管线
  3️⃣ 学习 OpenGL/Vulkan（3-6 月）
     - 绘制三角形
     - 纹理映射
     - 光照系统
  4️⃣ 学习 Shader 编程（持续）
     - GLSL/HLSL
     - 各种特效实现

专家路径（如果想成为引擎程序员）：
  1️⃣ 深入 C++（1 年+）
  2️⃣ 图形学精通（1 年+）
  3️⃣ 实现小型引擎（1 年+）
  4️⃣ 研究大型引擎源码（持续）
     - Unreal Engine 源码
     - Unity 开源部分


【推荐资源】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

初学者：
  📚 Unity 官方教程
  📚 Brackeys (YouTube)
  🎮 做小游戏练手

进阶：
  📚 《游戏引擎架构》
  📚 《Real-Time Rendering》（实时渲染）
  📚 LearnOpenGL.com

高级：
  📚 《Physically Based Rendering》（PBR 圣经）
  📚 SIGGRAPH 论文
  📚 GDC 技术分享


【现实建议】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果你：
• 想快速做游戏 → 学 Unity + C#（几个月见成果）
• 对图形感兴趣 → 学 C++ + OpenGL（1 年+）
• 想进大厂游戏组 → 两者都要会

记住：
  🎯 游戏开发 ≠ 图形编程
  🎯 大部分游戏程序员不写 Shader
  🎯 引擎程序员需要深入 C++ 和图形学
  🎯 选择适合自己的路径

你已经会 Java/Kotlin：
  ✅ 学 C# 很快（语法相似）
  ✅ 先用 Unity 做游戏（快速上手）
  ✅ 遇到性能问题再学 C++（针对性强）

)" << endl;
}

// ============================================
// Main
// ============================================

int main() {
    cout << "======================================================================" << endl;
    cout << "  游戏画面渲染机制详解" << endl;
    cout << "======================================================================" << endl;
    
    explain_game_rendering_stack();
    explain_honor_of_kings_architecture();
    explain_genshin_impact_architecture();
    compare_game_types();
    show_cpp_rendering_code();
    summary_and_learning_path();
    
    cout << "\n======================================================================" << endl;
    cout << "终极总结" << endl;
    cout << "======================================================================" << endl;
    
    cout << R"(
【问题：游戏画面是 C++ 做的吗？】

答案：✅ 是，但不是 C++ 直接画每个像素

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
完整流程：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C++ / C# (游戏代码)
  ↓ "露娜释放大招"
  
C++ (游戏引擎)
  ↓ "加载模型、特效、设置位置"
  
C++ (图形 API)
  ↓ "调用 OpenGL/DirectX"
  
C/C++ (GPU 驱动)
  ↓ "翻译成 GPU 指令"
  
Shader (GPU 程序)
  ↓ "计算每个像素颜色"
  
GPU (硬件)
  ↓ "并行渲染 200 万像素"
  
屏幕
  ✅ 你看到炫酷的大招画面！


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分工总结：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C++ 是"导演"：
  ✅ 决定画什么（哪些对象、在哪里）
  ✅ 管理资源（模型、纹理、动画）
  ✅ 优化性能（LOD、遮挡剔除）
  ✅ 调用图形 API

Shader 是"画师"：
  ✅ 计算光照（漫反射、镜面反射）
  ✅ 应用纹理（给模型贴皮肤）
  ✅ 实现特效（火焰、水面、辉光）
  ✅ 后处理（模糊、景深、调色）

GPU 是"画板"：
  ✅ 并行处理（同时画百万像素）
  ✅ 硬件加速（比 CPU 快几百倍）
  ✅ 实时渲染（60 帧/秒）


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
王者荣耀技术栈：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

客户端：
  • 引擎：Unity (C++ 核心 + C# 脚本)
  • 图形 API：OpenGL ES (移动端)
  • Shader：GLSL
  • GPU：手机芯片（高通/Mali/Apple）

服务器：
  • 游戏逻辑：C++ (高性能)
  • 不涉及画面渲染（服务器无 GPU）


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
给你的建议：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果你想做游戏：
  1️⃣ 先学 Unity + C#（快速上手，几个月见成果）
  2️⃣ 做几个小游戏练手
  3️⃣ 遇到性能问题再学 C++
  4️⃣ 对图形感兴趣再学 Shader

不要：
  ❌ 一开始就学底层图形学（太难，容易放弃）
  ❌ 纠结"用什么语言"（工具是为了解决问题）
  ❌ 追求完美（先做出能玩的游戏）

记住：
  🎮 游戏开发的乐趣在于创造
  🎮 画面只是表现形式
  🎮 好玩比好看更重要
  🎮 先用引擎做游戏，再深入底层

你的优势（已会 Java/Kotlin）：
  ✅ C# 语法相似，学起来很快
  ✅ 面向对象思维可以直接用
  ✅ Unity 生态丰富，资料多
  ✅ 从 Unity 入门是最佳选择！

)" << endl;
    
    cout << "======================================================================" << endl;
    
    return 0;
}

/**
 * 编译运行：
 * g++ -std=c++17 34_game_graphics_rendering.cpp -o game_rendering
 * ./game_rendering
 * 
 * 
 * 关键要点：
 * 
 * 1. 游戏画面渲染的层次：
 *    游戏逻辑 (C++/C#) 
 *    → 引擎 (C++) 
 *    → 图形 API (C++) 
 *    → GPU 驱动 (C++) 
 *    → Shader (GLSL/HLSL) 
 *    → GPU 硬件
 * 
 * 2. C++ 的角色：
 *    ✅ 游戏引擎核心（Unity、Unreal 底层）
 *    ✅ 调用图形 API（OpenGL、DirectX、Vulkan）
 *    ✅ 管理场景和资源
 *    ✅ 性能优化算法
 *    ❌ 不直接画每个像素（那是 GPU + Shader 的工作）
 * 
 * 3. 王者荣耀画面实现：
 *    客户端：Unity (C# 逻辑 + C++ 引擎) + OpenGL ES + Shader
 *    服务器：C++ (只处理逻辑，不渲染画面)
 * 
 * 4. 学习建议：
 *    入门：Unity + C# (快速)
 *    进阶：C++ + OpenGL (深入)
 *    专家：引擎开发 + Shader 编程
 * 
 * 5. 现实情况：
 *    - 90% 的游戏程序员不写底层渲染代码
 *    - 只有引擎程序员和图形程序员需要深入 C++ 图形学
 *    - 大部分人用现成引擎（Unity/Unreal）就够了
 */
