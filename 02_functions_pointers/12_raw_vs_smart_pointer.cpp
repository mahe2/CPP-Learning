/**
 * 裸指针 vs 智能指针：概念对比
 * 
 * 核心概念：
 * - 裸指针（Raw Pointer）：原始的 C/C++ 指针，需要手动管理内存
 * - 智能指针（Smart Pointer）：C++11引入的自动管理内存的指针
 */

#include <iostream>
#include <string>
#include <memory>

using namespace std;

// ============================================
// 什么是裸指针？
// ============================================
void what_is_raw_pointer() {
    cout << "\n" << string(60, '=') << endl;
    cout << "什么是裸指针（Raw Pointer）？" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\n定义：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "裸指针 = 原始指针 = 普通指针" << endl;
    cout << "就是 C/C++ 最基本的指针类型" << endl;
    cout << "用 * 声明，用 new/delete 管理内存" << endl;
    
    cout << "\n示例：" << endl;
    cout << "---------------------------------------" << endl;
    
    // 裸指针示例
    int* ptr1 = new int(42);           // 裸指针
    string* ptr2 = new string("Hello"); // 裸指针
    int* arr = new int[10];             // 裸指针数组
    
    cout << "int* ptr1 = new int(42);        ← 裸指针" << endl;
    cout << "string* ptr2 = new string();    ← 裸指针" << endl;
    cout << "int* arr = new int[10];         ← 裸指针数组" << endl;
    
    cout << "\n特点：" << endl;
    cout << "✓ 就是普通的指针变量" << endl;
    cout << "✓ 保存内存地址" << endl;
    cout << "✗ 不会自动释放内存" << endl;
    cout << "✗ 需要手动 delete" << endl;
    
    // 必须手动释放
    delete ptr1;
    delete ptr2;
    delete[] arr;
    
    cout << "\n必须手动 delete，否则内存泄漏！" << endl;
}

// ============================================
// 什么是智能指针？
// ============================================
void what_is_smart_pointer() {
    cout << "\n" << string(60, '=') << endl;
    cout << "什么是智能指针（Smart Pointer）？" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\n定义：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "智能指针 = 自动管理内存的指针" << endl;
    cout << "C++11 引入，封装在 <memory> 头文件中" << endl;
    cout << "本质是一个模板类，重载了 * 和 -> 运算符" << endl;
    
    cout << "\n三种智能指针：" << endl;
    cout << "---------------------------------------" << endl;
    
    // 智能指针示例
    unique_ptr<int> ptr1 = make_unique<int>(42);
    shared_ptr<string> ptr2 = make_shared<string>("Hello");
    weak_ptr<string> ptr3 = ptr2;
    
    cout << "1. unique_ptr<int>     ← 独占所有权" << endl;
    cout << "2. shared_ptr<string>  ← 共享所有权" << endl;
    cout << "3. weak_ptr<string>    ← 弱引用" << endl;
    
    cout << "\n特点：" << endl;
    cout << "✓ 自动管理内存" << endl;
    cout << "✓ 离开作用域自动释放" << endl;
    cout << "✓ 不需要手动 delete" << endl;
    cout << "✓ 防止内存泄漏" << endl;
    
    cout << "\n不需要 delete，自动释放！" << endl;
    // 离开作用域，自动释放内存
}

// ============================================
// 为什么叫"裸"指针？
// ============================================
void why_called_raw() {
    cout << "\n" << string(60, '=') << endl;
    cout << "为什么叫'裸'指针（Raw）？" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\n'裸'的含义：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "Raw = 原始的、未加工的、赤裸的" << endl;
    cout << "\n就像：" << endl;
    cout << "- Raw data = 原始数据（未处理的数据）" << endl;
    cout << "- Raw material = 原材料（未加工的材料）" << endl;
    cout << "- Raw pointer = 原始指针（未封装的指针）" << endl;
    
    cout << "\n对比：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "裸指针 = 原始指针：" << endl;
    cout << "  - 没有封装" << endl;
    cout << "  - 没有自动管理" << endl;
    cout << "  - 需要手动操作" << endl;
    cout << "  - 像'裸奔'一样，没有保护" << endl;
    
    cout << "\n智能指针 = 封装后的指针：" << endl;
    cout << "  - 有封装（类）" << endl;
    cout << "  - 自动管理" << endl;
    cout << "  - 有'衣服'保护" << endl;
    cout << "  - 更安全" << endl;
}

// ============================================
// 详细对比
// ============================================
class Resource {
private:
    string name;
public:
    Resource(string n) : name(n) {
        cout << "  📦 Resource 创建: " << name << endl;
    }
    ~Resource() {
        cout << "  💥 Resource 销毁: " << name << endl;
    }
    void use() {
        cout << "  🔧 使用 Resource: " << name << endl;
    }
};

void compare_raw_vs_smart() {
    cout << "\n" << string(60, '=') << endl;
    cout << "裸指针 vs 智能指针：详细对比" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\n--- 裸指针（手动管理）---" << endl;
    {
        Resource* raw = new Resource("裸指针资源");
        raw->use();
        delete raw;  // 必须手动释放
        cout << "  ⚠️  忘记 delete 会内存泄漏！" << endl;
    }
    
    cout << "\n--- 智能指针（自动管理）---" << endl;
    {
        auto smart = make_unique<Resource>("智能指针资源");
        smart->use();
        // 不需要 delete，自动释放
        cout << "  ✅ 自动释放，不会泄漏！" << endl;
    }
}

// ============================================
// 术语总结
// ============================================
void terminology_summary() {
    cout << "\n" << string(60, '=') << endl;
    cout << "C++ 指针术语总结" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\n1. 裸指针（Raw Pointer）：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "别名：" << endl;
    cout << "  - Raw pointer" << endl;
    cout << "  - Native pointer" << endl;
    cout << "  - Plain pointer" << endl;
    cout << "  - 原始指针" << endl;
    cout << "  - 普通指针" << endl;
    cout << "\n示例：" << endl;
    cout << "  int* ptr = new int(10);  ← 这就是裸指针" << endl;
    
    cout << "\n2. 智能指针（Smart Pointer）：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "三种类型：" << endl;
    cout << "  - unique_ptr (C++11)" << endl;
    cout << "  - shared_ptr (C++11)" << endl;
    cout << "  - weak_ptr (C++11)" << endl;
    cout << "\n示例：" << endl;
    cout << "  auto ptr = make_unique<int>(10);  ← 这是智能指针" << endl;
    
    cout << "\n3. 为什么叫'裸'？" << endl;
    cout << "---------------------------------------" << endl;
    cout << "因为智能指针出现后，为了区分：" << endl;
    cout << "  - 老式指针：裸指针（Raw，原始的）" << endl;
    cout << "  - 新式指针：智能指针（Smart，聪明的）" << endl;
    cout << "\n就像：" << endl;
    cout << "  - 裸机 vs 虚拟机" << endl;
    cout << "  - 裸金属 vs 云服务" << endl;
    cout << "  - Raw指针 vs Smart指针" << endl;
}

// ============================================
// 使用建议
// ============================================
void usage_recommendations() {
    cout << "\n" << string(60, '=') << endl;
    cout << "使用建议" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\n❌ 不推荐：裸指针" << endl;
    cout << "---------------------------------------" << endl;
    cout << "int* ptr = new int(10);" << endl;
    cout << "// ... 使用 ptr" << endl;
    cout << "delete ptr;  // 容易忘记，导致内存泄漏" << endl;
    
    cout << "\n问题：" << endl;
    cout << "1. 容易忘记 delete → 内存泄漏" << endl;
    cout << "2. 可能重复 delete → 程序崩溃" << endl;
    cout << "3. 异常时可能跳过 delete → 内存泄漏" << endl;
    cout << "4. 需要写析构函数、拷贝构造等（Rule of Five）" << endl;
    
    cout << "\n✅ 推荐：智能指针" << endl;
    cout << "---------------------------------------" << endl;
    cout << "auto ptr = make_unique<int>(10);" << endl;
    cout << "// ... 使用 ptr" << endl;
    cout << "// 自动释放，不需要 delete" << endl;
    
    cout << "\n优势：" << endl;
    cout << "1. 自动释放内存 → 不会泄漏" << endl;
    cout << "2. 不会重复释放 → 更安全" << endl;
    cout << "3. 异常安全 → 自动清理" << endl;
    cout << "4. 不需要写析构函数（Rule of Zero）" << endl;
    
    cout << "\n⚠️  特殊情况：可以用裸指针" << endl;
    cout << "---------------------------------------" << endl;
    cout << "1. 不拥有所有权的指针（只是观察，不负责释放）" << endl;
    cout << "2. 与 C 语言库交互" << endl;
    cout << "3. 性能极其关键的场景（智能指针有微小开销）" << endl;
    cout << "4. 底层系统编程" << endl;
}

// ============================================
// 与其他语言对比
// ============================================
void compare_languages() {
    cout << "\n" << string(60, '=') << endl;
    cout << "与 Java/Kotlin 对比" << endl;
    cout << string(60, '=') << endl;
    
    cout << "\nJava/Kotlin：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "- 没有'指针'的概念（对用户隐藏）" << endl;
    cout << "- 所有对象引用都由 GC 管理" << endl;
    cout << "- 不需要手动释放内存" << endl;
    cout << "\nString str = new String(\"Hello\");" << endl;
    cout << "// 不需要 delete，GC 自动回收" << endl;
    
    cout << "\nC++ 裸指针：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "- 需要手动管理内存" << endl;
    cout << "- 需要手动 delete" << endl;
    cout << "\nstring* str = new string(\"Hello\");" << endl;
    cout << "delete str;  // 必须手动删除" << endl;
    
    cout << "\nC++ 智能指针：" << endl;
    cout << "---------------------------------------" << endl;
    cout << "- 自动管理内存" << endl;
    cout << "- 不需要手动 delete" << endl;
    cout << "- 更接近 Java/Kotlin 的体验" << endl;
    cout << "\nauto str = make_unique<string>(\"Hello\");" << endl;
    cout << "// 不需要 delete，自动释放" << endl;
    
    cout << "\n总结：" << endl;
    cout << "智能指针让 C++ 更接近 Java/Kotlin 的便利性，" << endl;
    cout << "同时保留了 C++ 的性能和控制力！" << endl;
}

int main() {
    cout << "===========================================" << endl;
    cout << "  裸指针 vs 智能指针" << endl;
    cout << "===========================================" << endl;
    
    what_is_raw_pointer();
    what_is_smart_pointer();
    why_called_raw();
    compare_raw_vs_smart();
    terminology_summary();
    usage_recommendations();
    compare_languages();
    
    cout << "\n===========================================" << endl;
    cout << "核心总结" << endl;
    cout << "===========================================" << endl;
    cout << "✅ 裸指针 = 原始指针 = 普通指针" << endl;
    cout << "   - C/C++ 基本的指针类型" << endl;
    cout << "   - 需要手动 new/delete" << endl;
    cout << "   - int* ptr = new int(10);" << endl;
    cout << "\n✅ 智能指针 = 自动管理内存的指针" << endl;
    cout << "   - C++11 引入" << endl;
    cout << "   - 自动释放内存" << endl;
    cout << "   - auto ptr = make_unique<int>(10);" << endl;
    cout << "\n✅ '裸'是相对于'智能'而言的" << endl;
    cout << "   - Raw（原始的）vs Smart（聪明的）" << endl;
    cout << "   - 就像：裸机 vs 虚拟机" << endl;
    cout << "\n现代C++推荐：优先使用智能指针！" << endl;
    cout << "===========================================" << endl;
    
    return 0;
}

/**
 * 术语对照表：
 * 
 * ┌─────────────────┬──────────────────┬─────────────────┐
 * │  中文           │  英文            │  示例            │
 * ├─────────────────┼──────────────────┼─────────────────┤
 * │ 裸指针          │ Raw Pointer      │ int* ptr;       │
 * │ 原始指针        │ Native Pointer   │ char* str;      │
 * │ 普通指针        │ Plain Pointer    │ double* arr;    │
 * │ 智能指针        │ Smart Pointer    │ unique_ptr      │
 * │ 唯一指针        │ Unique Pointer   │ unique_ptr<T>   │
 * │ 共享指针        │ Shared Pointer   │ shared_ptr<T>   │
 * │ 弱引用指针      │ Weak Pointer     │ weak_ptr<T>     │
 * └─────────────────┴──────────────────┴─────────────────┘
 * 
 * 记忆技巧：
 * - Raw（裸）= 原始的、未封装的、需要手动管理
 * - Smart（智能）= 封装好的、自动管理的、更安全
 * 
 * 类比：
 * - 裸指针 = 手动挡汽车（需要手动操作）
 * - 智能指针 = 自动挡汽车（自动管理）
 * 
 * C++ 发展史：
 * - C++98: 只有裸指针 + auto_ptr（有缺陷）
 * - C++11: 引入 unique_ptr, shared_ptr, weak_ptr
 * - 术语"裸指针"是为了区分新的智能指针而产生的
 */
