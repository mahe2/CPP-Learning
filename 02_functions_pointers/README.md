# 02 - 函数和指针

## 📚 本章内容

学习 C++ 的函数、指针和引用 - 这是与 Java/Kotlin 最大的区别。

### 示例文件

1. **01_functions_basics.cpp** - 函数定义、调用、默认参数
2. **02_pointers_basics.cpp** - 指针基础（重点！）
3. **03_references.cpp** - 引用和参数传递
4. **04_dynamic_memory.cpp** - 动态内存分配（new/delete）

---

## 🚀 如何编译运行

```bash
# 编译单个文件
g++ -std=c++17 01_functions_basics.cpp -o functions
./functions

# 编译所有示例
g++ -std=c++17 02_pointers_basics.cpp -o pointers
./pointers
```

---

## ⚠️ 重要概念（必须掌握）

### 1. 指针 (Pointer)
```cpp
int num = 42;
int* ptr = &num;  // ptr 存储 num 的地址
*ptr = 100;       // 通过指针修改 num 的值
```

**理解要点：**
- `&` 取地址符
- `*` 解引用符
- 指针存储内存地址
- Java/Kotlin 的引用类型本质上是指针，但由 JVM 管理

### 2. 引用 (Reference)
```cpp
int original = 10;
int& ref = original;  // ref 是 original 的别名
ref = 20;             // original 也变为 20
```

**理解要点：**
- 引用是变量的别名
- 必须初始化
- 不能重新绑定
- 比指针更安全

### 3. 参数传递
```cpp
void byValue(int x);         // 按值传递（复制）
void byRef(int& x);          // 按引用传递（可修改）
void byPointer(int* x);      // 按指针传递（可修改）
void readOnly(const int& x); // 常量引用（只读，高效）
```

**对比 Java:**
- Java 基本类型：按值传递
- Java 对象：按引用传递（实际是引用的值）
- C++ 可以精确控制传递方式

### 4. 动态内存
```cpp
int* ptr = new int(42);   // 分配
delete ptr;               // 释放

int* arr = new int[10];   // 数组
delete[] arr;             // 释放数组
```

**重要！**
- Java/Kotlin 有 GC，自动回收
- C++ 必须手动 `delete`
- 忘记 `delete` 会导致内存泄漏
- 现代 C++ 推荐用智能指针（后续章节）

---

## 💡 与 Java/Kotlin 对比

| 特性 | Java/Kotlin | C++ |
|------|-------------|-----|
| 内存管理 | 自动 GC | 手动 new/delete |
| 指针 | 隐式（引用） | 显式指针 |
| 参数传递 | 值/引用（自动） | 值/引用/指针（手动） |
| 引用 | 所有对象 | 可选使用 |
| 空指针 | null | nullptr |

---

## 🎯 学习重点

### 第一优先级（必须理解）
1. **指针基础**：`&` 取地址，`*` 解引用
2. **引用**：别名的概念
3. **按值 vs 按引用传递**
4. **动态内存**：new/delete 配对

### 第二优先级（深入理解）
1. 指针算术
2. 指针和数组的关系
3. 常量指针 vs 指向常量的指针
4. 内存泄漏的避免

---

## 🐛 常见错误

### 1. 忘记释放内存
```cpp
// ❌ 错误
int* ptr = new int(10);
// 忘记 delete ptr;

// ✅ 正确
int* ptr = new int(10);
delete ptr;
ptr = nullptr;
```

### 2. delete 和 delete[] 混用
```cpp
// ❌ 错误
int* arr = new int[10];
delete arr;  // 应该用 delete[]

// ✅ 正确
int* arr = new int[10];
delete[] arr;
```

### 3. 悬空指针
```cpp
// ❌ 错误
int* ptr = new int(10);
delete ptr;
*ptr = 20;  // 未定义行为！

// ✅ 正确
int* ptr = new int(10);
delete ptr;
ptr = nullptr;  // 设为空指针
```

### 4. 引用未初始化
```cpp
// ❌ 错误
int& ref;  // 编译错误！引用必须初始化

// ✅ 正确
int num = 10;
int& ref = num;
```

---

## 📖 练习建议

1. **理解内存布局**：画图理解栈和堆
2. **实验指针**：写代码观察地址变化
3. **对比 Java**：思考 Java 对象引用和 C++ 指针的异同
4. **练习传参**：体会三种参数传递方式的区别

### 推荐练习
```cpp
// 练习1：交换两个变量
void swap(int& a, int& b);

// 练习2：动态数组操作
int* createArray(int size);
void fillArray(int* arr, int size);
void printArray(const int* arr, int size);

// 练习3：字符串操作（使用指针）
int stringLength(const char* str);
void copyString(char* dest, const char* src);
```

---

## 🔥 面试常见问题

1. **指针和引用的区别？**
2. **什么是悬空指针？如何避免？**
3. **栈和堆的区别？**
4. **内存泄漏的原因和预防？**
5. **为什么 C++ 需要手动管理内存？**

---

**下一章：面向对象编程 ➡️**
