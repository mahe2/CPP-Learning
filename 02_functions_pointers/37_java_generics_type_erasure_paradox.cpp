/*
 * Java 泛型的类型擦除悖论
 * 问题: 为什么不支持基本类型？既然运行时擦除了类型，编译时为什么不能用基本类型？
 * 核心: 这是 Java 向后兼容导致的设计缺陷
 */

#include <iostream>
#include <vector>
#include <string>
using namespace std;

// ============================================================================
// 一、问题重现：看似矛盾的设计
// ============================================================================

void demonstrate_paradox() {
    cout << "\n=== 1. Java 泛型的悖论 ===" << endl;
    
    cout << "\nJava 代码示例 (伪代码):" << endl;
    cout << "  List<Integer> list = new ArrayList<>();  // ✅ 必须用包装类" << endl;
    cout << "  List<int> list = new ArrayList<>();      // ❌ 不能用基本类型" << endl;
    
    cout << "\n编译后的字节码:" << endl;
    cout << "  List list = new ArrayList();  // 类型信息被擦除！" << endl;
    
    cout << "\n悖论:" << endl;
    cout << "  🤔 既然运行时都变成 List (没有类型参数)" << endl;
    cout << "  🤔 为什么编译时不能写 List<int>？" << endl;
    cout << "  🤔 反正运行时都会擦除，为什么要强制装箱？" << endl;
    
    cout << "\n答案: 这是 Java 为了向后兼容做出的妥协！" << endl;
}

// ============================================================================
// 二、类型擦除的实现机制（问题的根源）
// ============================================================================

void explain_type_erasure_mechanism() {
    cout << "\n=== 2. 类型擦除的实现机制 ===" << endl;
    
    cout << "\nJava 泛型的编译过程:" << endl;
    cout << "\n步骤1: 源代码（编译前）" << endl;
    cout << "  List<String> list = new ArrayList<>();" << endl;
    cout << "  list.add(\"hello\");" << endl;
    cout << "  String s = list.get(0);" << endl;
    
    cout << "\n步骤2: 类型检查（编译时）" << endl;
    cout << "  编译器检查: add(\"hello\") ✅ String类型正确" << endl;
    cout << "  编译器检查: list.add(123) ❌ 不是String，拒绝编译" << endl;
    
    cout << "\n步骤3: 类型擦除（生成字节码）" << endl;
    cout << "  List list = new ArrayList();        // 擦除 <String>" << endl;
    cout << "  list.add((Object)\"hello\");        // 转换为 Object" << endl;
    cout << "  String s = (String)list.get(0);    // 插入强制转换" << endl;
    
    cout << "\n关键: 擦除后所有对象都变成 Object 类型！" << endl;
    cout << "  - Object 是引用类型（指针）" << endl;
    cout << "  - int/double 是基本类型（值）" << endl;
    cout << "  - 两者不兼容！" << endl;
}

// ============================================================================
// 三、为什么不支持基本类型：技术原因
// ============================================================================

void explain_why_no_primitives() {
    cout << "\n=== 3. 为什么不支持基本类型：技术原因 ===" << endl;
    
    cout << "\n原因1: Object 不能存储基本类型" << endl;
    cout << "  如果允许 List<int>，擦除后变成:" << endl;
    cout << "    List list = new ArrayList();" << endl;
    cout << "    list.add((Object)42);  // ❌ int 不能转换为 Object" << endl;
    cout << "\n  Java 的 Object 只能存储引用类型，不能存储值类型" << endl;
    
    cout << "\n原因2: 内存布局不同" << endl;
    cout << "  引用类型 (Integer):  8字节指针 → 堆上对象(16字节)" << endl;
    cout << "  基本类型 (int):      4字节值，直接存储" << endl;
    cout << "  擦除后统一用 Object，无法处理基本类型的值语义" << endl;
    
    cout << "\n原因3: null 语义冲突" << endl;
    cout << "  Object obj = null;  // ✅ 引用类型可以为 null" << endl;
    cout << "  int value = null;   // ❌ 基本类型不能为 null" << endl;
    cout << "  擦除后无法区分 null 和基本类型" << endl;
    
    cout << "\nC++ 没有这个问题:" << endl;
    cout << "  vector<int> v;  // 直接生成 int 版本的代码" << endl;
    cout << "  不需要 Object，不需要装箱，不需要擦除" << endl;
}

// ============================================================================
// 四、向后兼容：历史包袱
// ============================================================================

void explain_backward_compatibility() {
    cout << "\n=== 4. 向后兼容：历史包袱 ===" << endl;
    
    cout << "\nJava 泛型的历史:" << endl;
    cout << "  1995年: Java 1.0 发布，没有泛型" << endl;
    cout << "    ArrayList list = new ArrayList();" << endl;
    cout << "    list.add(\"hello\");  // 存储为 Object" << endl;
    cout << "    String s = (String)list.get(0);  // 手动转换" << endl;
    
    cout << "\n  2004年: Java 5 引入泛型，必须向后兼容" << endl;
    cout << "    要求: 新代码能在旧 JVM 上运行" << endl;
    cout << "    要求: 旧代码能和新代码互操作" << endl;
    
    cout << "\n兼容性约束导致的设计:" << endl;
    cout << "  ✅ 编译时增加类型检查（提高安全性）" << endl;
    cout << "  ✅ 运行时擦除类型（保持字节码兼容）" << endl;
    cout << "  ❌ 必须使用引用类型（因为擦除到 Object）" << endl;
    cout << "  ❌ 不能用基本类型（Object 不支持）" << endl;
    
    cout << "\n如果 Java 从头设计泛型（不考虑兼容性）:" << endl;
    cout << "  可能会像 C++ 一样，为每个类型生成代码" << endl;
    cout << "  就能支持基本类型了" << endl;
}

// ============================================================================
// 五、为什么 C++ 可以支持基本类型
// ============================================================================

// C++ 模板：为每个类型生成独立代码
template<typename T>
class MyContainer {
private:
    T* data;
    int size_;
    int capacity;
public:
    MyContainer() : data(nullptr), size_(0), capacity(0) {}
    
    void add(T value) {
        // 直接存储 T 类型的值，不需要 Object
        cout << "  存储类型 " << typeid(T).name() << " 的值" << endl;
    }
    
    T get(int index) {
        return data[index];  // 直接返回 T 类型，不需要转换
    }
};

void explain_cpp_solution() {
    cout << "\n=== 5. 为什么 C++ 可以支持基本类型 ===" << endl;
    
    cout << "\nC++ 模板的编译过程:" << endl;
    
    cout << "\n源代码:" << endl;
    cout << "  MyContainer<int> intContainer;" << endl;
    cout << "  MyContainer<string> strContainer;" << endl;
    
    cout << "\n编译器生成的代码（代码生成，不是擦除）:" << endl;
    cout << "  // 为 int 生成专门的类" << endl;
    cout << "  class MyContainer_int {" << endl;
    cout << "      int* data;  // 直接用 int，不是 Object" << endl;
    cout << "      void add(int value) { ... }" << endl;
    cout << "      int get(int index) { ... }" << endl;
    cout << "  };" << endl;
    
    cout << "\n  // 为 string 生成另一个类" << endl;
    cout << "  class MyContainer_string {" << endl;
    cout << "      string* data;  // 直接用 string" << endl;
    cout << "      void add(string value) { ... }" << endl;
    cout << "      string get(int index) { ... }" << endl;
    cout << "  };" << endl;
    
    cout << "\n实际演示:" << endl;
    MyContainer<int> intCont;
    intCont.add(42);
    
    MyContainer<double> doubleCont;
    doubleCont.add(3.14);
    
    MyContainer<string> strCont;
    strCont.add("hello");
    
    cout << "\n关键区别:" << endl;
    cout << "  Java: 擦除到统一的 Object → 不支持基本类型" << endl;
    cout << "  C++:  为每个类型生成代码 → 支持任何类型" << endl;
}

// ============================================================================
// 六、装箱的代价
// ============================================================================

void demonstrate_boxing_cost() {
    cout << "\n=== 6. 装箱的代价 ===" << endl;
    
    cout << "\nJava 必须装箱的开销:" << endl;
    cout << "\n内存开销:" << endl;
    cout << "  int value = 42;          // 4 字节" << endl;
    cout << "  Integer obj = 42;        // 16 字节对象 + 8 字节指针 = 24 字节" << endl;
    cout << "  开销: 6倍内存" << endl;
    
    cout << "\n性能开销:" << endl;
    cout << "  int sum = 0;" << endl;
    cout << "  for (int i = 0; i < 1000000; i++) {" << endl;
    cout << "      sum += i;  // 直接加法，1条CPU指令" << endl;
    cout << "  }" << endl;
    
    cout << "\n  Integer sum = 0;" << endl;
    cout << "  for (int i = 0; i < 1000000; i++) {" << endl;
    cout << "      sum += i;  // 拆箱 → 加法 → 装箱 → GC，慢10倍" << endl;
    cout << "  }" << endl;
    
    cout << "\nC++ 无装箱开销:" << endl;
    vector<int> vec;
    for (int i = 0; i < 10; i++) {
        vec.push_back(i);  // 直接存储 int，零开销
    }
    cout << "  vector<int> 直接存储基本类型，无装箱" << endl;
    
    cout << "\n性能对比:" << endl;
    cout << "  Java ArrayList<Integer>: 100万整数 = 16MB + GC开销" << endl;
    cout << "  C++ vector<int>:         100万整数 = 4MB + 无GC" << endl;
}

// ============================================================================
// 七、其他语言的解决方案
// ============================================================================

void other_languages_solutions() {
    cout << "\n=== 7. 其他语言的解决方案 ===" << endl;
    
    cout << "\nC# (.NET) 的解决方案:" << endl;
    cout << "  问题: C# 也有 Object，也需要兼容旧代码" << endl;
    cout << "  解决: 运行时支持泛型（Reified Generics）" << endl;
    cout << "    List<int> → 运行时保留类型信息" << endl;
    cout << "    List<int> 和 List<string> 是不同的类" << endl;
    cout << "  结果: ✅ 支持基本类型，无需装箱" << endl;
    
    cout << "\nC++ 的解决方案:" << endl;
    cout << "  没有历史包袱，直接用代码生成" << endl;
    cout << "  vector<int> 编译时生成专门代码" << endl;
    cout << "  结果: ✅ 支持任何类型，零开销" << endl;
    
    cout << "\nJava 的困境:" << endl;
    cout << "  - 选择了类型擦除（向后兼容 JVM）" << endl;
    cout << "  - 擦除到 Object（不支持基本类型）" << endl;
    cout << "  - 无法改变（数百万行旧代码依赖）" << endl;
    cout << "  结果: ❌ 永远不支持基本类型" << endl;
}

// ============================================================================
// 八、Java 的补救措施
// ============================================================================

void java_workarounds() {
    cout << "\n=== 8. Java 的补救措施 ===" << endl;
    
    cout << "\n补救1: 自动装箱/拆箱 (Java 5)" << endl;
    cout << "  List<Integer> list = new ArrayList<>();" << endl;
    cout << "  list.add(42);         // 自动装箱: Integer.valueOf(42)" << endl;
    cout << "  int x = list.get(0);  // 自动拆箱: obj.intValue()" << endl;
    cout << "  缺点: 隐藏了性能开销，容易踩坑" << endl;
    
    cout << "\n补救2: 特化的基本类型流 (Java 8)" << endl;
    cout << "  IntStream, LongStream, DoubleStream" << endl;
    cout << "  IntStream.range(0, 1000000).sum();  // 无装箱" << endl;
    cout << "  缺点: 只支持 int/long/double，不是通用方案" << endl;
    
    cout << "\n补救3: Project Valhalla (未来)" << endl;
    cout << "  目标: 值类型 (Value Types)" << endl;
    cout << "  可能在 Java 20+ 引入" << endl;
    cout << "  但仍无法改变现有泛型的限制" << endl;
}

// ============================================================================
// 九、设计教训
// ============================================================================

void design_lessons() {
    cout << "\n=== 9. 设计教训 ===" << endl;
    
    cout << "\nJava 泛型的设计权衡:" << endl;
    cout << "  优点:" << endl;
    cout << "    ✅ 向后兼容（旧代码能运行）" << endl;
    cout << "    ✅ 编译时类型安全（减少错误）" << endl;
    cout << "    ✅ 字节码体积小（只有一份代码）" << endl;
    
    cout << "\n  缺点:" << endl;
    cout << "    ❌ 不支持基本类型（必须装箱）" << endl;
    cout << "    ❌ 运行时无类型信息（instanceof 受限）" << endl;
    cout << "    ❌ 性能开销（装箱/拆箱/GC）" << endl;
    cout << "    ❌ 无法创建泛型数组（new T[10]）" << endl;
    
    cout << "\nC++ 模板的设计权衡:" << endl;
    cout << "  优点:" << endl;
    cout << "    ✅ 支持任何类型（基本类型/类）" << endl;
    cout << "    ✅ 零开销抽象（编译时优化）" << endl;
    cout << "    ✅ 保留类型信息（typeid 可用）" << endl;
    cout << "    ✅ 编译时计算（元编程）" << endl;
    
    cout << "\n  缺点:" << endl;
    cout << "    ❌ 编译时间长（每个类型生成代码）" << endl;
    cout << "    ❌ 二进制体积大（代码膨胀）" << endl;
    cout << "    ❌ 错误信息难懂（模板实例化错误）" << endl;
    
    cout << "\n教训:" << endl;
    cout << "  🎓 向后兼容是双刃剑：保护旧代码，限制新设计" << endl;
    cout << "  🎓 早期设计决策影响深远：Java 选择了 Object，永远受限" << endl;
    cout << "  🎓 没有完美方案：Java 选兼容，C++ 选性能" << endl;
}

// ============================================================================
// 十、核心总结
// ============================================================================

void summary() {
    cout << "\n========================================" << endl;
    cout << "           核心总结" << endl;
    cout << "========================================" << endl;
    
    cout << "\n问题: Java 泛型为什么不支持基本类型？" << endl;
    cout << "  既然运行时擦除了类型，为什么编译时不能用基本类型？" << endl;
    
    cout << "\n答案: 类型擦除的技术限制 + 历史包袱" << endl;
    
    cout << "\n技术原因:" << endl;
    cout << "  1️⃣ 擦除后统一用 Object" << endl;
    cout << "     - Object 只能存储引用类型（对象指针）" << endl;
    cout << "     - int/double 是值类型，不能转换为 Object" << endl;
    
    cout << "\n  2️⃣ 内存布局不兼容" << endl;
    cout << "     - 引用类型: 指针（8字节）→ 对象（堆上）" << endl;
    cout << "     - 基本类型: 值（4字节），直接存储" << endl;
    cout << "     - 擦除后无法统一处理" << endl;
    
    cout << "\n  3️⃣ null 语义冲突" << endl;
    cout << "     - Object 可以为 null" << endl;
    cout << "     - int 不能为 null" << endl;
    cout << "     - 擦除后无法区分" << endl;
    
    cout << "\n历史原因:" << endl;
    cout << "  1️⃣ 向后兼容约束" << endl;
    cout << "     - Java 5 之前没有泛型，用 Object 存储" << endl;
    cout << "     - 引入泛型时必须兼容旧 JVM" << endl;
    cout << "     - 选择了类型擦除方案" << endl;
    
    cout << "\n  2️⃣ 设计权衡" << endl;
    cout << "     - 优先保证兼容性" << endl;
    cout << "     - 牺牲了对基本类型的支持" << endl;
    cout << "     - 这是无法改变的历史遗留问题" << endl;
    
    cout << "\n对比 C++:" << endl;
    cout << "  ✅ 没有历史包袱，从头设计" << endl;
    cout << "  ✅ 用代码生成，不用类型擦除" << endl;
    cout << "  ✅ 为每个类型生成专门代码" << endl;
    cout << "  ✅ 支持任何类型（基本类型/类）" << endl;
    cout << "  ✅ 零开销抽象" << endl;
    
    cout << "\n关键理解:" << endl;
    cout << "  \"运行时擦除类型\" 不等于 \"可以用任何类型\"" << endl;
    cout << "  擦除到 Object 是限制，不是自由" << endl;
    cout << "  因为 Object 只支持引用类型！" << endl;
    
    cout << "\n记忆口诀:" << endl;
    cout << "  擦除到 Object，Object 是引用" << endl;
    cout << "  基本类型是值，两者不兼容" << endl;
    cout << "  必须装箱成 Integer，才能当 Object" << endl;
    cout << "  这是 Java 的历史包袱，无法改变" << endl;
    
    cout << "\n========================================" << endl;
}

// ============================================================================
// 主函数
// ============================================================================

int main() {
    cout << "\n╔══════════════════════════════════════════════════════════╗" << endl;
    cout << "║   Java 泛型的类型擦除悖论                                ║" << endl;
    cout << "║   为什么不支持基本类型？编译时为什么不能用 int？         ║" << endl;
    cout << "╚══════════════════════════════════════════════════════════╝" << endl;
    
    // 1. 问题重现
    demonstrate_paradox();
    
    // 2. 类型擦除机制
    explain_type_erasure_mechanism();
    
    // 3. 技术原因
    explain_why_no_primitives();
    
    // 4. 历史原因
    explain_backward_compatibility();
    
    // 5. C++ 的解决方案
    explain_cpp_solution();
    
    // 6. 装箱代价
    demonstrate_boxing_cost();
    
    // 7. 其他语言
    other_languages_solutions();
    
    // 8. Java 的补救
    java_workarounds();
    
    // 9. 设计教训
    design_lessons();
    
    // 10. 总结
    summary();
    
    return 0;
}
